import numpy as np
import math

## recomposition totale de chaikin
def recomposition(array) :
    x_array = np.array([])
    y_array = np.array([])
    size = len(array) - 1

    #for index in range(0, size):


    #res = 